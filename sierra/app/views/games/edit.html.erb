<%= render "games/header" %>
<h3>Edit Game</h3>

<%= render 'form' %>


<%= link_to 'Delete Game', [ @game ],
	method: :delete,
	data: { confirm: 'Are you sure?' } %> 
<%= link_to 'Back', games_path %>
